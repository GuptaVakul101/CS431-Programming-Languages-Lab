package problem1;

// global constants which are used frequently
public class Constants {
    // code for NULL sock means no sock is picked up
    static final int NULL_SOCK_CODE = -1;
    // different code for different color socks
    static final int BLUE_SOCK_CODE = 1;
    static final int GREY_SOCK_CODE = 2;
    static final int WHITE_SOCK_CODE = 3;
    static final int BLACK_SOCK_CODE = 4;
}
